# SEAL - Colab T4 GPU Requirements
# Optimized for Google Colab T4 (16GB VRAM)
# NO OpenAI API - FREE models only

# Core ML Libraries
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
datasets>=2.14.0
peft>=0.7.0
bitsandbytes>=0.41.0

# Training & Optimization
sentencepiece>=0.1.99
protobuf>=3.20.0
safetensors>=0.4.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
pillow>=10.0.0
scipy>=1.11.0

# No vLLM (doesn't work well on T4)
# No OpenAI (costs money)
# No ZMQ (not needed for single-GPU setup)
